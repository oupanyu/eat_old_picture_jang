package top.eopj.client.config;

public class EOPJConfig {
    private boolean isReplaceBackgroundEnabled = false;

    public boolean isReplaceBackgroundEnabled() {
        return isReplaceBackgroundEnabled;
    }

    public void setReplaceBackgroundEnabled(boolean replaceBackgroundEnabled) {
        isReplaceBackgroundEnabled = replaceBackgroundEnabled;
    }
}
